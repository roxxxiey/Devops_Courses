import math
import json

badDefault = []


def BrokenFunction(a, b, c, d, e, f, list=[]):
    try:
        x = a + b
    except Exception:
        pass
    unused_value = c + d + e + f
    second_unused = math.pi
    third_unused = badDefault
    return list
